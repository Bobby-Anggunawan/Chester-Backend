# Optional file for deploying to Google App Engine Flexible Environment.
runtime: aspnetcore
env: flex

# Remember - storing secrets in plaintext is potentially unsafe. Consider using
# something like https://cloud.google.com/secret-manager/docs/overview to help keep
# secrets secret.
env_variables:
    DB_USER: root
    DB_PASS: x&c%QrzPBEF'AZlH
    DB_NAME: chester
    INSTANCE_HOST: cloudsql
    INSTANCE_CONNECTION_NAME: chester-game:us-central1:chester-db-mysql

beta_settings:
    cloud_sql_instances: "chester-game:us-central1:chester-db-mysql"